import { StyleSheet } from "react-native";

// Essa função esta responsavel pela estilização do titulo
const styles = StyleSheet.create({
    boxTitle: {
        alignItems: "center",
        justifyContent: "center",
        padding: 10,
    },
    textTitle: {
        color: "#010469",
        fontSize: 24,
        fontWeight: "bold", 
    }
});

export default styles